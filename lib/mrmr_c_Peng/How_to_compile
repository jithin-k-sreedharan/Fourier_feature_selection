To compile just run the following command (suppose you have a GNU type c/c++ compiler):

make -f mrmr.makefile

Jithin's additions:
Instead of the above command, use: bash compile.sh
Sample run: ./mrmr -i sample.csv -n 10

Then you can run the mrmr command like

./mrmr
